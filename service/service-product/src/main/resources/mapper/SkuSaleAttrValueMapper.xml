<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atguigu.gmall.product.mapper.SkuSaleAttrValueMapper">

    <select id="getSkuValueIdsMap" resultType="java.util.Map">
        select sv.sku_id, group_concat(sv.sale_attr_value_id ORDER BY ssav.base_sale_attr_id ASC SEPARATOR '|')
            value_ids
        from sku_sale_attr_value sv
        inner join spu_sale_attr_value ssav on sv.sale_attr_value_id = ssav.id
        where sv.spu_id = #{spuId}
        group by sv.sku_id;
    </select>
</mapper>